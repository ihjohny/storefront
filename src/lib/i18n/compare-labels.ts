/** Mirrors `catalog.compare` in locale JSON — imported where JSON types are shallow. */
export type ProductCompareLabels = {
  add: string;
  added: string;
  removeColumn: string;
  maxReached: string;
  trayTitle: string;
  trayAction: string;
  trayClear: string;
  pageTitle: string;
  pageDescription: string;
  emptyState: string;
  tableVariant: string;
  tableSku: string;
  tablePrice: string;
  tableCategories: string;
  tableSummary: string;
  tableVendor: string;
  clearAll: string;
  loadError: string;
};
